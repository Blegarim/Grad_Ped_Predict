# EvalCfg — replaces test.py:308-322 literals.
batch_size: 16
num_workers: 4
model_type: full
bench_context_scale: 3.0     # deliberate synthetic-benchmark scale (FLOPs/latency only)
bench_img_size: 128
latency_trials: 50
threshold_sweep_lo: 0.10
threshold_sweep_hi: 0.90
threshold_sweep_step: 0.05
