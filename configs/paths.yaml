# PathsCfg — run-relative artifact locations (no absolute paths in code).
pie_root: data                   # PIE toolkit data_path (cloned into this repo); images under pie_root/images/...
sequences_dir: data/sequences    # generate_sequences pkl output home (gitignored under data/)
lmdb_train: [preprocessed_train, preprocessed_train_aug]
lmdb_train_balanced: [preprocessed_train_balanced]
lmdb_val: preprocessed_val
lmdb_test: preprocessed_test
log_dir: training_log            # legacy flat dirs (kept for reading OLD artifacts)
ckpt_dir: best_model_outputs     # legacy flat dirs
run_ckpt_dir: model_outputs      # legacy flat dirs
runs_dir: outputs/runs           # new per-run home: outputs/runs/{run_id}/ (Prompt 0.3)
